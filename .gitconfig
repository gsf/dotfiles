[user]
  name = Gabriel Farrell
  email = g@gsf.io
  username = gsf
[core]
  excludesfile = ~/.gitignore
  pager =
[alias]
  ci = commit
  co = checkout
  st = status -sb
  ready = rebase -i @{u}
  tree = log --oneline --graph --decorate --all --color
[push]
  default = current
