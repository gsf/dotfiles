[user]
  name = Gabriel Farrell
  email = g@grrawr.com
[core]
  excludesfile = ~/.gitignore
  editor = vim
[alias]
  st = status -sb
  ready = rebase -i @{u}
  tree = log --oneline --graph --decorate --all --color
[push]
  default = current
