[user]
  name = Gabriel Farrell
  email = g@grrawr.com
[core]
  excludesfile = ~/.gitignore
[alias]
  ci = commit
  st = status -sb
  ready = rebase -i @{u}
  tree = log --oneline --graph --decorate --all --color
[push]
  default = current
